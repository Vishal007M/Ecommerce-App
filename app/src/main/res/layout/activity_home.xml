<?xml version="1.0" encoding="utf-8"?>
<androidx.drawerlayout.widget.DrawerLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/drawer_layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <include
        android:id="@+id/app_bar_dashboard"
        layout="@layout/app_bar_main"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <!-- <com.google.android.material.navigation.NavigationView
         android:id="@+id/nav_drawer"
         android:layout_width="match_parent"
         android:layout_height="match_parent"
         android:layout_gravity="start"
         android:background="@color/white"
         android:clipToPadding="false"
         android:fitsSystemWindows="true"
         android:padding="0dp"
         android:theme="@style/NavigationView"
         app:headerLayout="@layout/header_drawer"
         app:itemBackground="@drawable/nav_highlight_background"
         app:itemHorizontalPadding="15dp"
         app:itemIconPadding="10dp"
         app:itemIconSize="20dp"
         app:itemIconTint="@drawable/popup_menu_color"
         app:itemTextColor="@drawable/popup_menu_color"
         app:itemVerticalPadding="10dp"
         app:menu="@menu/drawer_nav"
         android:layoutDirection="ltr">

     </com.google.android.material.navigation.NavigationView>-->

    <com.google.android.material.navigation.NavigationView
        android:id="@+id/navDrawer"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_gravity="end"
        android:background="@color/white"
        android:fitsSystemWindows="true">

        <include
            android:id="@+id/nevBarDrawer"
            layout="@layout/naviagtion_drawer" />

    </com.google.android.material.navigation.NavigationView>

</androidx.drawerlayout.widget.DrawerLayout>